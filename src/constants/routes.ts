export const ROUTES = {
  home: '/',
  register: '/register',
  profile: '/profile',
  branches: '/branches',
  items: '/items',
};

export const PROTECTED_ROUTES = [
  ROUTES.home,
  ROUTES.profile,
  ROUTES.branches,
  ROUTES.items,
];
